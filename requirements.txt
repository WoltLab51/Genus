pytest>=7.0.0
pytest-asyncio>=0.21.0
fastapi>=0.100.0
httpx>=0.24.0
cryptography>=41.0.0
aiofiles>=23.0.0
PyYAML>=6.0.1

# LLM Provider (optional — only needed when the respective provider is used)
openai>=1.0.0
